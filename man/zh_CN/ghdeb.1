.TH GHDEB 1 "2026-08-02" "ghdeb 0.6.0" "用户命令"
.SH 名称
ghdeb \- 管理从 GitHub Releases 下载的 .deb 包
.SH 概要
.B ghdeb
.I 命令
.RI [ 参数 ]
.SH 描述
.B ghdeb
管理从 GitHub Releases 下载的 .deb 包。
它在一个命令中处理架构检测、资源匹配、下载和安装。
.SH 命令
.SS install
.B ghdeb install
.IB owner / repo [ @tag ]
.br
.B ghdeb install
.I 短名称
.PP
安装或升级包到指定版本。如未指定 tag，则使用最新稳定版。
支持使用目录中的短名称（如
.IR bat 、
.IR fd ）。
.SS upgrade
.B ghdeb upgrade
.RI [ owner/repo ]
.PP
升级已管理的包。如未指定包名，则升级所有已管理的包。升级前自动扫描 orphan 包。
.SS reinstall
.B ghdeb reinstall
.I 包名
.PP
强制重新安装指定包，下载并安装最新 release。历史记录标记为重装。
.SS search
.B ghdeb search
.I 关键词
.PP
在包目录中搜索，支持按名称、显示名、简介匹配。支持正则表达式。
.SS update
.B ghdeb update
.RB [ \-\-verbose ]
.PP
加
.BR \-\-verbose
时，逐行显示后台步骤（目录加载、每包检查的缓存命中/拉取版本、.deb 资产校验、
快照组装），替代紧凑的进度条。
.PP
类
.B apt update
。查询目录中各 GitHub 仓库的最新版本、本地已装版本并判定是否可升级，
校验并移除无当前架构 .deb 包的目录条目，将结果写入单一系统级缓存文件
(/var/cache/ghdeb/cache.json)。与
.B apt update
类似，需要写入 root 所有的系统缓存，可能提示输入
.BR sudo
密码。
之后
.B ghdeb list
只读取该快照，不再实时查询网络。
.SS list
.B ghdeb list
.RB [ \-\-json ]
.PP
列出包目录中的所有条目，仅读取
.B ghdeb update
生成的本地快照文件，不访问网络。显示短名称、仓库、已装版本和最新版本；
若已装版本低于最新版本，条目会标记为可升级。未运行过 update 时相关版本列显示 "-"。
.TP
.B \-\-json
以 JSON 格式输出列表。

.SS catalog
.B ghdeb catalog
.I 子命令
.PP
管理包目录。目录将短名称映射到 GitHub 仓库（或直接 .deb URL），
用于短名称安装。
.RS
.B ghdeb list
列出目录中的所有条目。
.TP
.B ghdeb catalog show
.I 名称
显示目录条目的详细信息。
.TP
.B ghdeb catalog search
.I 关键词
搜索目录（与顶层
.B search
命令相同）。
.TP
.B ghdeb catalog add
.I 名称
.B \-\-repo
.I owner/repo
.RB [ \-\-url
.I deb-url ]
.RB [ \-\-pretty-name
.I 名称 ]
.RB [ \-\-website
.I url ]
.RB [ \-\-summary
.I 简介 ]
.RB [ \-\-gpg-key
.I url ]
向系统目录
.RI ( /etc/ghdeb/catalog.toml )
添加条目。
.B \-\-repo
和
.B \-\-url
至少需要指定一个。若指定
.B \-\-url
，则包可从直接 .deb 下载 URL 安装（非 GitHub 来源）。
.B \-\-gpg-key
指定 GPG 公钥 URL，用于签名校验。
.TP
.B ghdeb catalog delete
.I 名称
从系统目录中删除条目。
.TP
.TP
.B ghdeb catalog modify
.I 名称
.B \-\-repo
.I owner/repo
修改已有系统目录条目对应的仓库。
.TP
.B ghdeb catalog validate
.RB [ 名称 ]
.RB [ \-\-all ]
校验目录条目，移除未提供当前架构 .deb 的条目。不指定参数时校验并
清洗全部条目；指定名称时仅校验该条目。
.RE
.SS show / info
.B ghdeb show
.I 包名
.PP
显示仓库的最新 release 信息，包括匹配当前架构的下载资源。
.B info
是
.B show
的别名。
.SS history
.B ghdeb history
.IB owner / repo
.PP
显示某个包的完整操作历史（安装、升级、卸载）。
.SS purge
.B ghdeb purge
.I 包名
.PP
使用
.B apt-get purge
卸载软件包，并用
.B autoremove
清理依赖。同时移除 ghdeb 管理记录。
.SS clean
.B ghdeb clean
.RB [ \-\-dry\-run ]
.PP
清理
.B /var/cache/ghdeb/
中缓存的 .deb 文件。使用
.B \-\-dry\-run
可预览将被删除的文件而不实际删除。
.SS version
.B ghdeb version
.PP
显示 ghdeb 版本。
.SH 目录系统
包目录采用 TOML 格式，存放于系统路径
.B /etc/ghdeb/catalog.toml
，内置 curated 的 GitHub .deb 包列表。
.PP
目录条目格式：
.EX
[bat]
repo = "sharkdp/bat"
pretty_name = "bat"
website = "https://github.com/sharkdp/bat"
summary = "A cat(1) clone with syntax highlighting"
.EE
.PP
非 GitHub 来源可使用直接 URL 模板：
.EX
[myapp]
url = "https://example.com/releases/myapp_{version}_{arch}.deb"
pretty_name = "我的应用"
summary = "自定义应用"
.EE
.PP
URL 占位符：
.B {version}
（去掉前导 v 的版本号）、
.B {arch}
（dpkg 架构名称）。
.SH GPG 校验
当目录条目包含
.B gpg_key
字段时，ghdeb 会在下载 .deb 包后使用指定的 GPG 公钥尝试验证签名。
若校验失败，安装将被中止。
.SH 架构匹配
.B ghdeb
通过
.B dpkg \-\-print\-architecture
自动检测系统架构，并按文件名模式匹配 release 资源：
.TS
tab(@);
l l.
系统架构@匹配关键词
amd64@amd64, x86_64, x86-64, x64
arm64@arm64, aarch64
armhf@armhf, armv7l, armv7
i386@i386, x86, i686, 386
.TE
.SH 缓存
.B ghdeb
将每个目录包的状态（已装、已装版本、GitHub 最新版本、可升级）扁平化
缓存在单个文件
.B /var/cache/ghdeb/cache.json
中，TTL 为 24 小时。下载的 .deb 文件缓存在
.B /var/cache/ghdeb/
中。
.SH 状态文件
已管理包的记录存储在
.B /var/cache/ghdeb/history.json
中。
.SH 环境变量
.TP
.B GITHUB_TOKEN
.TQ
.B GH_TOKEN
GitHub 个人访问令牌。将 API 速率限制从 60 次/小时（未认证）提高到 5,000 次/小时。
.SH 语言环境
输出语言由
.B LANG
环境变量决定。中文 (zh_*) 产生中文输出；其他所有 locale 产生英文输出。
.SH 文件
.TP
.B /etc/ghdeb/catalog.toml
系统包目录（内置 curated 的 GitHub .deb 包列表）。
.TP
.B /var/cache/ghdeb/cache.json
扁平化统一缓存：每个目录包一条记录（已装状态、已装/最新版本、可升级，24 小时 TTL）。
.TP
.B /var/cache/ghdeb/*.deb
已下载的 .deb 包缓存。
.TP
.B /var/cache/ghdeb/history.json
已管理包的状态记录。
.SH 退出状态
.TP
.B 0
成功。
.TP
.B 1
错误（参数无效、网络失败、安装失败等）。
.SH 示例
.TP
.B ghdeb install bat
通过短名称安装 bat。
.TP
.B ghdeb install LeisureLinux/ghdeb
安装 ghdeb 最新版。
.TP
.B ghdeb install LeisureLinux/ghdeb@v0.6.0
安装 ghdeb 指定版本。
.TP
.B ghdeb update
刷新各包最新/已装版本信息到本地快照。
.TP
.B ghdeb upgrade
升级所有已管理的包。
.TP
.B ghdeb search monitor
搜索目录中包含 "monitor" 的包。
.TP
.B ghdeb list
列出所有目录条目。
.TP
.B ghdeb catalog add myapp \-\-repo user/myapp \-\-summary "我的应用"
向系统目录添加自定义条目。
.TP
.B ghdeb catalog add myapp \-\-url "https://example.com/myapp_{version}_{arch}.deb"
添加非 GitHub 来源到目录。
.TP
.B ghdeb catalog delete myapp
从系统目录删除自定义条目。
.TP
.B ghdeb clean \-\-dry\-run
预览将被清理的缓存文件。
.TP
.B ghdeb purge rustdesk
卸载 rustdesk 并清理。
.SH SHELL 补全
.B ghdeb
提供 bash、zsh 和 fish 的补全脚本。安装后新 shell 自动生效。
.PP
.B bash:
source /usr/share/bash-completion/completions/ghdeb
.br
或重启 shell。
.PP
.B zsh:
运行
.I compinit
或重启 shell。补全安装在
.IR /usr/share/zsh/site-functions/_ghdeb 。
.PP
.B fish:
补全安装在
.IR /usr/share/fish/vendor_completions.d/ghdeb.fish 。
.SH 参见
.BR dpkg (1),
.BR apt (8)
.SH 作者
由 LeisureLinux 编写。
.SH 报告错误
在 <https://github.com/LeisureLinux/ghdeb/issues> 报告问题
